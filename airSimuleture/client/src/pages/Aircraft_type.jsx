import React from 'react'
import { useNavigate } from 'react-router'

export default function Aircraft_type() {
    //  const navigate = useNavigate()
    // if (!token) {
    //     return navigate('/')
    // }
  return (
    <div>Aircraft_type</div>
  )
}
